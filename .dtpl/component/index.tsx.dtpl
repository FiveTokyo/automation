/**
 * ${ModuleName}
 * @author ${ref.author}
 * @date ${datetime}
 * @since ${pkg.version}
 */

import React from 'react'
// import classnames from 'classnames'
// import styles from './index.module.scss'

export interface ${DirName}Props {}

function ${DirName}(_props: ${DirName}Props) {
    return <div>${DirName}</div>
}

export default React.memo(${DirName})

